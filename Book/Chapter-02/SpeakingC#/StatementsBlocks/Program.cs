using System;
using System.Security.Cryptography.X509Certificates; // A semicolon is used to terminate a statement in C#. It indicates the end of a complete instruction or command. In this example, the semicolon is used to terminate the statement that writes "Hello World!" to the console.

namespace StatementsBlocks
{ // An open brace indicates the start of a block of code. In this example, the open brace is used to define the scope of the namespace "StatementsBlocks" and the class "Program". It signifies that the code enclosed within the braces belongs to that namespace and class.
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!"); // A statement is a complete instruction that performs an action. In this example, the statement is "Console.WriteLine("Hello World!");" which writes the string "Hello World!" to the console. The semicolon at the end of the statement indicates that it is complete and can be executed
        }
    }
} // A close brace inidicates the end of a block of code. In this example, the close brace is used to signify the end of the "Program" class and the "StatementsBlocks" namespace. It indicates that the code enclosed within the braces has been completed and is no longer part of that class or namespace.
